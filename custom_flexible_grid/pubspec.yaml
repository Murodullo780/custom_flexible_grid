name: custom_flexible_grid
description: A customizable, flexible grid widget that supports both vertical and horizontal layouts with scroll and builder capabilities.
version: 1.0.1
homepage: https://github.com/Murodullo780/custom_flexible_grid
repository: https://github.com/Murodullo780/custom_flexible_grid
issue_tracker: https://github.com/Murodullo780/custom_flexible_grid/issues

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter